{% extends 'base.html.twig' %}

{% block title %}Bienvenue dans votre profil{% endblock %}

{% block content %}
    <h1>Mon compte</h1>
    Bienvenue {{ app.user.firstname }} dans votre compte. C'est dans cet espace que vous allez pouvoir gérer toutes vos infos persos.
    <hr>
    <a href="{{ path('account_password') }}"> Modifier mon mot de passe</a><br/>
    <a href="{{ path('account_address') }}"> Modifier mes adresses</a><br/>
    <a href="{{ path('account_order') }}"> Mes commandes</a><br/>
    {% if is_granted('ROLE_ADMIN') %}

        <a href="{{ path('admin') }}"> Vers le panneau admin </a>

    {% endif %}
{% endblock %}
